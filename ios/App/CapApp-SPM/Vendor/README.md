Place `AmigoFaceSwapSDK.xcframework` here.

On macOS, run:

```bash
npm run install:amigo-sdk
```

This copies `~/Downloads/AmigoFaceSwapSDK.xcframework.zip` into this folder as:

`Vendor/AmigoFaceSwapSDK.xcframework`
